# Step 1 — plan 스캐폴드

**목표**: `plans/dev-medisearch-freetext/` 디렉토리 + `index.json` + step 파일 생성.

**산출물**: index.json (5 steps), step1~5.md spec 파일.

**검증**: doc-only, `/verify --skip "plan scaffold"`
