# 1.1.9 수집 스케줄 — 누락 Celery 태스크
상태: planning
마지막 수정: 2026-04-17

## 범위
- 설계 문서: `docs/1_programming/1.1_metadata/1.1.9_ingestion_schedule.md`
- 핵심 기능: 1.1.9 수집 매트릭스 기준 10개 태스크 중 누락 6개 구현

## 현재 구현 vs 목표

| 태스크 | 현재 | 목표 |
|--------|------|------|
| `poll_cp_emails` | ✅ | — |
| `sync_kobis` | ✅ | — |
| `sync_tmdb` | ✅ | — |
| `enrich_content_metadata` | ✅ | — |
| `reeval_quality_scores` | ✅ | — |
| (6번째 확인 필요) | ✅? | workers/tasks/metadata.py 확인 |
| `process_ingest_metadata` | ❌ | 인제스트 AX 이벤트 수신 트리거 |
| `sync_ott_popularity` | ❌ | OTT TOP10 스텁 (크롤링 ToS 이슈로 stub) |
| `reeval_thumbnail_quality` | ❌ | VAMS 썸네일 재평가 (1.1.6_vams 선행) |
| `sync_distribution_status` | ❌ | 외부 배포 동기화 스텁 |
| `apply_deletion_policy` | ❌ | 만료 이미지 삭제 정책 |
| `aggregate_daily_stats` | ❌ | 일간 통계 집계 |

## 패턴 참조 파일
- `backend/workers/tasks/metadata.py` — 기존 태스크 패턴
- `backend/workers/celery_app.py` — Beat 스케줄 등록

## 세션 분할

### 세션 1 — 기존 태스크 현황 확인 + 단독 태스크 구현
범위:
- `workers/tasks/metadata.py` 현재 구현된 6번째 태스크 확인
- `process_ingest_metadata` — 인제스트 완료 이벤트 수신 (Celery signal 또는 API 훅)
- `aggregate_daily_stats` — 일간 처리 건수·품질 평균 집계
- `apply_deletion_policy` — 만료 이미지 삭제 (ContentImage.expires_at 기준)
- Beat 스케줄 등록 (celery_app.py)
- 완료 기준: `python3 -c "from workers.tasks.metadata import aggregate_daily_stats; print('OK')"`

### 세션 2 — 스텁 태스크 + VAMS 연계 태스크
범위:
- `sync_ott_popularity` — 스텁 (로그만, 실제 크롤링 미구현)
- `sync_distribution_status` — 스텁 (Phase 2 외부 배포 연동 후 구현)
- `reeval_thumbnail_quality` — 1.1.6_vams 완료 후 구현 (현재 스텁)
- Beat 스케줄 등록

## 완료 기준
- [ ] `workers/tasks/metadata.py`에 6개 태스크 추가
- [ ] `celery_app.py` Beat 스케줄 10개 등록 확인
- [ ] `python3 -c "from workers.tasks.metadata import *; print('OK')"`
- [ ] `workers/CLAUDE.md` 태스크 목록 갱신
