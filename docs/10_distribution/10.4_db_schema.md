# 10.4 배포/송출 관리 — DB 스키마

## 테이블 목록 (5개)

### distribution_jobs — 배포 작업

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID PK | |
| content_id | UUID FK | 콘텐츠 ID |
| platform | VARCHAR | gtv / mobile_ios / mobile_android / web / ott |
| status | VARCHAR | pending / deploying / success / failed |
| cdn_url | TEXT | 배포된 CDN URL |
| deployed_at | TIMESTAMP | 배포 완료 시각 |
| error_message | TEXT | 오류 메시지 (실패 시) |
| created_at | TIMESTAMP | |

### purge_requests — 퍼지 요청 이력

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID PK | |
| content_id | UUID FK | 대상 콘텐츠 (선택) |
| urls | TEXT[] | 퍼지 대상 URL 목록 |
| trigger_type | VARCHAR | meta_update / image_replace / delete / price_change / manual |
| status | VARCHAR | requested / processing / completed / failed |
| requested_by | UUID FK | 요청자 (users) |
| cdn_response | JSONB | CDN API 응답 |
| requested_at | TIMESTAMP | |
| completed_at | TIMESTAMP | |

### portal_sync_logs — 포털 데이터 전송 이력

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID PK | |
| content_id | UUID FK | |
| sync_type | VARCHAR | realtime / batch |
| data_type | VARCHAR | meta / price / image / banner / category |
| status | VARCHAR | success / failed |
| payload_snapshot | JSONB | 전송 데이터 스냅샷 |
| synced_at | TIMESTAMP | |
| error_detail | TEXT | |

### da_submissions — DA 소재 전송 이력

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID PK | |
| campaign_id | UUID FK | 마케팅 캠페인 ID |
| asset_id | UUID FK | 디자인 소재 ID |
| platform | VARCHAR | internal / google / kakao / naver |
| status | VARCHAR | pending / submitted / live / error |
| submit_response | JSONB | 광고 플랫폼 응답 |
| submitted_at | TIMESTAMP | |
| go_live_at | TIMESTAMP | 게재 시작 시각 |

### rating_image_status — 영등위 이미지 배포 상태

| 컬럼 | 타입 | 설명 |
|------|------|------|
| id | UUID PK | |
| content_id | UUID FK | |
| rating_code | VARCHAR | G / 12 / 15 / 18 / EXEMPT |
| image_url | TEXT | 등급 이미지 CDN URL |
| platform | VARCHAR | 플랫폼 구분 |
| is_valid | BOOLEAN | 이미지 정상 여부 |
| last_checked_at | TIMESTAMP | 마지막 점검 시각 |
| error_type | VARCHAR | not_found / mismatch / broken_url |

*작성일: 2026-04-09*
