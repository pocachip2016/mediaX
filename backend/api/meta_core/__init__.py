"""
meta_core — Layer 1: canonical content metadata

어느 서비스(service_kt, Dam 등)에서도 참조 가능한 기본 메타 권위 모듈.
Content master, Person, Taxonomy, Image, ExternalMetaSource, TMDB cache.

서비스 도메인(ContentMetadata, ContentDistribution, ServiceCategory 등)은
api/programming/ 또는 api/service_kt/ 에 위치.
"""
